
#include "rtp_network/fec/fecdecoder.h"
#include "rtp_network/fec/fecencoder.h"
#include <gtest/gtest.h>

/**
 * 用于测试FEC的编解码是否正常
 */

TEST(FECTest,Encoder){
    EXPECT_TRUE(true);
}

TEST(FECTest,Decoder){
    EXPECT_TRUE(true);
}

TEST(FECTest,Codec){
    EXPECT_TRUE(true);
}
